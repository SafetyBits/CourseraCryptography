package week2;

public class Q8 {
	public static void main(String[] args) {
		System.out
				.println("The most direct computation would be for the enemy to try all 2^r possible keys, one by one."
						.toCharArray().length);
		System.out
				.println("To consider the resistance of an enciphering process to being broken we should assume that at same times the enemy knows everything but the key being used and to break it needs only discover the key from this information."
						.toCharArray().length);
		System.out
				.println("The significance of this general conjecture, assuming its truth, is easy to see. It means that it may be feasible to design ciphers that are effectively unbreakable."
						.toCharArray().length);
		System.out
				.println("In this letter I make some remarks on a general principle relevant to enciphering in general and my machine."
						.toCharArray().length);
	}
}
